branch:
  main:
    buildPath: /

build:
  commands:
    npm: npm ci

env:
  variables:
    NODE_ENV: production

artifacts:
  baseDirectory: .next
  files:
    - '**/*'

cache:
  paths:
    - 'node_modules/**/*'
